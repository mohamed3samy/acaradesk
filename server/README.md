
## 🚀 Getting Strated

To get started with backend, follow these steps:

1. Clone this repository to your local machine.
2. Run `npm install` to install the required dependencies.
3. Run `npm run server` to start the web server. 
